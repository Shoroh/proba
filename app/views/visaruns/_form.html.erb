<%= form_for(@visarun) do |f| %>
  <% if @visarun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visarun.errors.count, "error") %> prohibited this visarun from being saved:</h2>

      <ul>
      <% @visarun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
